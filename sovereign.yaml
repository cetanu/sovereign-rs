templates: []
sources:
  - type: python
    config:
      code: |
        import json

        d = dict(
          a = 1,
          b = 2,
          c = dict(
            foo = "baz"
          )
        )

        def main():
            return json.dumps(d)

